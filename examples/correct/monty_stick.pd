{
  int d; int g; int o;
  d ?= { 0, 1, 2 }; # Pick winning door
  g ?= { 0, 1, 2 }; # Pick guessed door
  o ?= { 0, 1, 2 }; # Pick empty door

  while ((o == g) || (o == d)) {
    o = (o + 1) % 3;
  }

  print g == d;
}